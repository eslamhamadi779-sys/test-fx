
/* ============================================================
   # [AR] إعادة تعيين التنسيقات العامة للمتصفحات (CSS Reset)
   # [EN] Global CSS Reset & Box Sizing Core Rules
   ============================================================ */
*,
*::before,
*::after {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

/* ============================================================
   # [AR] المتغيرات الأساسية وجذور التنسيق (Root Variables & Base)
   # [EN] Central Root Variables, Typography & Main Body Configurations
   ============================================================ */
:root {
    --bg-color: #0b0c10;
    --container-bg: #1f2833;
    --text-gold: #c5a880;
    --neon-blue: #66fcf1;
    --danger-red: #ff4d4d;
    --teal-color: #2b7a78;
    --font-ar: 'Cairo', sans-serif;
}

html {
    scroll-behavior: smooth;
}

body {
    background-color: var(--bg-color);
    color: #ffffff;
    font-family: var(--font-ar);
    margin: 0;
    padding: 0;
    overflow-x: hidden;
}

html[dir="ltr"] { 
    --font-ar: 'Montserrat', sans-serif; 
}

/* ============================================================
   # [AR] التنسيق الخاص بالهيدر العلوي وأزرار التسجيل
   # [EN] Top Navigation Header & Auth Actions Styles
   ============================================================ */
header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 15px 5%;
    background-color: rgba(31, 40, 51, 0.9);
    backdrop-filter: blur(10px);
    border-bottom: 2px solid #1f2833;
    gap: 10px;
    flex-wrap: wrap;
}

.logo { 
    font-size: 1.5rem; 
    font-weight: 900; 
    color: var(--text-gold); 
}

/* أزرار التسجيل في الهيدر */
.auth-header-actions {
    display: flex;
    align-items: center;
    gap: 10px;
}

.auth-btn {
    border: none;
    padding: 8px 16px;
    font-weight: 700;
    cursor: pointer;
    border-radius: 5px;
    transition: 0.3s;
    font-size: 0.85rem;
    display: flex;
    align-items: center;
    gap: 6px;
    font-family: var(--font-ar);
}

.google-btn {
    background-color: #ea4335;
    color: #ffffff;
}

.google-btn:hover {
    background-color: #d93025;
    box-shadow: 0 0 10px rgba(234, 67, 53, 0.5);
}

.phone-btn {
    background-color: var(--teal-color);
    color: #ffffff;
}

.phone-btn:hover {
    background-color: #236462;
    box-shadow: 0 0 10px rgba(43, 122, 120, 0.5);
}

.user-badge {
    display: flex;
    align-items: center;
    gap: 10px;
    background: rgba(197, 168, 128, 0.15);
    padding: 5px 12px;
    border-radius: 20px;
    border: 1px solid var(--text-gold);
    color: var(--text-gold);
    font-weight: bold;
    font-size: 0.85rem;
}

.logout-btn {
    background: transparent;
    border: none;
    color: var(--danger-red);
    cursor: pointer;
    font-size: 0.85rem;
}

.lang-btn {
    background: transparent;
    border: 2px solid var(--text-gold);
    color: var(--text-gold);
    padding: 8px 16px;
    font-weight: 600;
    cursor: pointer;
    border-radius: 5px;
    transition: 0.3s;
}

.lang-btn:hover { 
    background-color: var(--text-gold); 
    color: var(--bg-color); 
}

/* ============================================================
   # [AR] واجهة الهيرو الرئيسية وأزرار تلجرام التفاعلية المزدوجة
   # [EN] Hero Landing Section & Telegram Dual CTA Interactive Buttons
   ============================================================ */
#hero {
    display: flex;
    justify-content: center;
    align-items: center;
    text-align: center;
    min-height: 50vh;
    background: linear-gradient(rgba(11,12,16,0.6), rgba(11,12,16,0.9)), url('https://i.postimg.cc/fyfYP5LN/2385aef2-576c-4c3e-b299-412e7edd6d48.jpg') no-repeat center/cover;
    padding: 40px 20px;
}

.hero-title { 
    font-size: 2.3rem; 
    font-weight: 900; 
    margin-bottom: 20px; 
    text-shadow: 0 0 15px rgba(0,0,0,0.8); 
}

.hero-magic { 
    color: var(--text-gold); 
}

.hero-sub { 
    font-size: 1.3rem; 
    color: #a4b3b6; 
    margin-bottom: 30px; 
    text-shadow: 0 0 10px rgba(0,0,0,0.8); 
}

.hero-buttons-container {
    display: flex;
    gap: 15px;
    justify-content: center;
    flex-wrap: wrap;
    margin-top: 10px;
}

#hero-telegram-btn {
    background: linear-gradient(45deg, #0088cc, #00a6ff);
    color: #ffffff;
    border: none;
    padding: 15px 35px;
    font-size: 1.1rem;
    font-weight: 700;
    border-radius: 50px;
    cursor: pointer;
    box-shadow: 0 4px 15px rgba(0, 136, 204, 0.4);
    transition: 0.3s;
    text-decoration: none;
    display: inline-block;
}

#hero-telegram-btn:hover { 
    transform: translateY(-3px); 
    box-shadow: 0 6px 25px rgba(0, 136, 204, 0.7); 
}

#hero-channel-btn {
    background: linear-gradient(45deg, #c5a880, #e6dcc7);
    color: #0b0c10;
    border: none;
    padding: 15px 35px;
    font-size: 1.1rem;
    font-weight: 700;
    border-radius: 50px;
    cursor: pointer;
    box-shadow: 0 4px 15px rgba(197, 168, 128, 0.4);
    transition: 0.3s;
    text-decoration: none;
    display: inline-block;
}

#hero-channel-btn:hover { 
    transform: translateY(-3px); 
    box-shadow: 0 6px 25px rgba(197, 168, 128, 0.7); 
}

/* ============================================================
   # [AR] لوحة التحكم الذكية وتخطيط كروت البيانات الحية
   # [EN] Live Dashboard Grid Layout & Responsive Card Systems
   ============================================================ */
.live-dashboard {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 20px;
    padding: 20px 5%;
}

.dashboard-card {
    background-color: var(--container-bg);
    border-radius: 10px;
    padding: 20px;
    border: 1px solid rgba(102, 252, 241, 0.1);
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
}

/* ============================================================
   # [AR] كارت الرصد الفلكي وتنسيقات مجسم وجدول القمر الرقمي
   # [EN] Lunar Astronomic Card & Calendar Grid
   ============================================================ */
.moon-status {
    width: 100%;
    text-align: center;
}

.moon-graphic-container {
    width: 120px;
    height: 120px;
    background: #000;
    border-radius: 50%;
    position: relative;
    margin: 15px auto;
    overflow: hidden;
    box-shadow: inset -10px -10px 20px rgba(0,0,0,0.8), 0 0 15px rgba(197, 168, 128, 0.4);
}

.moon-light {
    position: absolute;
    top: 0; left: 0; width: 100%; height: 100%;
    background: radial-gradient(circle, #fffde1 0%, #c5a880 70%);
    border-radius: 50%;
    transition: all 0.5s ease;
}

.moon-shadow {
    position: absolute;
    top: 0;
    height: 100%;
    width: 100%;
    border-radius: 50%;
    background: var(--bg-color);
    transition: .6s;
}

.liquidity-badge {
    background-color: rgba(102, 252, 241, 0.1);
    color: var(--neon-blue);
    padding: 8px 15px;
    border-radius: 20px;
    display: inline-block;
    font-weight: bold;
    margin-top: 10px;
}

.date-display { 
    font-size: 1.1rem; 
    color: var(--text-gold); 
    font-weight: bold; 
    margin-top: 5px; 
}

.moon-calendar {
    display: grid;
    grid-template-columns: repeat(7, 1fr);
    gap: 6px;
    margin-top: 20px;
}

.day-box {
    --card-dark: #111;
    padding: 10px;
    border-radius: 10px;
    text-align: center;
    font-size: 14px;
    border: 1px solid rgba(255, 255, 255, .1);
    background: var(--card-dark);
}

.today {
    border: 2px solid #66fcf1;
    box-shadow: 0 0 10px #66fcf1;
}

.full-moon {
    background: var(--text-gold);
    color: #000;
}

.dark-moon {
    background: #300;
}

.new-moon {
    background: var(--teal-color);
}

.balanced-moon {
    background: rgba(102, 252, 241, 0.08);
    border-color: var(--neon-blue);
}

/* ============================================================
   # [AR] عجلة الاقتران الكوني التفاعلية
   # [EN] Cosmic Planetary Wheel Engine
   ============================================================ */
.cosmic-container { 
    width: 100%; 
    text-align: center; 
    padding: 10px; 
}

.cosmic-wheel {
    width: 200px; height: 200px;
    border: 3px dashed var(--text-gold);
    border-radius: 50%;
    margin: 15px auto;
    position: relative;
    transition: transform 0.1s linear;
    background: radial-gradient(circle, rgba(31,40,51,0.6) 0%, rgba(11,12,16,0.9) 100%);
}

.center-earth {
    position: absolute; top: calc(50% - 15px); left: calc(50% - 15px);
    width: 30px; height: 30px; background: #2b7a78; border-radius: 50%;
    box-shadow: 0 0 8px #66fcf1;
}

.orbit-object { 
    position: absolute; 
    border-radius: 50%; 
    font-size: 1.1rem; 
    width: 24px; 
    height: 24px; 
    display: flex; 
    align-items: center; 
    justify-content: center;
}

#cosmic-sun { color: #ffaa00; text-shadow: 0 0 10px #ffaa00; top: 10px; left: calc(50% - 12px); }
#cosmic-moon { color: #ffffcc; text-shadow: 0 0 10px #fff; bottom: 10px; left: calc(50% - 12px); }
#cosmic-mars { color: #ff4d4d; text-shadow: 0 0 10px #ff4d4d; left: 10px; top: calc(50% - 12px); }
#cosmic-jupiter { color: #c5a880; text-shadow: 0 0 10px #c5a880; right: 10px; top: calc(50% - 12px); }

.wheel-slider {
    width: 80%; -webkit-appearance: none; background: var(--container-bg);
    height: 6px; border-radius: 3px; outline: none; margin: 15px 0;
    border: 1px solid var(--text-gold);
}

.wheel-slider::-webkit-slider-thumb {
    -webkit-appearance: none; width: 20px; height: 20px; border-radius: 50%;
    background: var(--text-gold); cursor: pointer; box-shadow: 0 0 8px var(--neon-blue);
}

/* ============================================================
   # [🔥 جديد] منتدى ملك ملوك الجان للتوصيات والتفاعلات
   # [EN] King Tykeel Recommendation Forum & Post Cards
   ============================================================ */
.forum-section {
    background-color: rgba(31, 40, 51, 0.7);
    padding: 30px 5%;
    margin: 30px 5%;
    border-radius: 12px;
    border: 1px solid rgba(197, 168, 128, 0.3);
}

.forum-header {
    text-align: center;
    margin-bottom: 25px;
}

.forum-header h2 {
    font-size: 1.8rem;
    color: var(--text-gold);
    margin-bottom: 8px;
}

.guest-notice-card {
    background: linear-gradient(135deg, rgba(31, 40, 51, 0.9), rgba(11, 12, 16, 0.9));
    border: 2px dashed var(--text-gold);
    border-radius: 10px;
    padding: 30px;
    text-align: center;
    margin: 20px 0;
}

.guest-notice-card .lock-icon {
    font-size: 2.5rem;
    color: var(--text-gold);
    margin-bottom: 15px;
}

.guest-notice-card h3 {
    margin-bottom: 10px;
    color: #ffffff;
}

.guest-notice-card p {
    color: #a4b3b6;
    max-width: 600px;
    margin: 0 auto 20px auto;
}

.action-btn {
    background: linear-gradient(45deg, var(--text-gold), #e6dcc7);
    color: #0b0c10;
    border: none;
    padding: 12px 30px;
    font-size: 1rem;
    font-weight: bold;
    border-radius: 25px;
    cursor: pointer;
    transition: 0.3s;
}

.action-btn:hover {
    box-shadow: 0 0 15px rgba(197, 168, 128, 0.6);
    transform: translateY(-2px);
}

/* صندوق النشر المخصص */
.publish-box {
    background: var(--container-bg);
    border-radius: 10px;
    padding: 20px;
    margin-bottom: 30px;
    border: 1px solid var(--neon-blue);
}

.publish-box h3 {
    margin-bottom: 15px;
    color: var(--neon-blue);
    font-size: 1.2rem;
}

.publish-box textarea {
    width: 100%;
    min-height: 100px;
    background: #0b0c10;
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: 8px;
    padding: 12px;
    color: #ffffff;
    font-family: var(--font-ar);
    font-size: 0.95rem;
    resize: vertical;
    outline: none;
    margin-bottom: 15px;
}

.publish-box textarea:focus {
    border-color: var(--text-gold);
}

.file-upload-wrapper {
    margin-bottom: 15px;
}

.file-label {
    display: inline-block;
    padding: 10px 18px;
    background: rgba(102, 252, 241, 0.1);
    color: var(--neon-blue);
    border: 1px dashed var(--neon-blue);
    border-radius: 6px;
    cursor: pointer;
    font-size: 0.9rem;
    transition: 0.3s;
}

.file-label:hover {
    background: rgba(102, 252, 241, 0.2);
}

.file-upload-wrapper input[type="file"] {
    display: none;
}

.image-preview-container img {
    max-width: 200px;
    max-height: 150px;
    border-radius: 8px;
    margin-top: 10px;
    border: 1px solid var(--text-gold);
}

.submit-post-btn {
    background: var(--teal-color);
    color: #ffffff;
    border: none;
    padding: 10px 25px;
    border-radius: 5px;
    font-weight: bold;
    cursor: pointer;
    transition: 0.3s;
}

.submit-post-btn:hover {
    background: #1e5a58;
}

/* كروت التوصيات والمنشورات */
.post-card {
    background: var(--container-bg);
    border-radius: 10px;
    padding: 20px;
    margin-bottom: 20px;
    border-right: 4px solid var(--text-gold);
}

.post-author {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 12px;
}

.author-info {
    display: flex;
    align-items: center;
    gap: 10px;
}

.author-avatar {
    width: 40px;
    height: 40px;
    border-radius: 50%;
    background: var(--text-gold);
    color: #0b0c10;
    display: flex;
    align-items: center;
    justify-content: center;
    font-weight: bold;
}

.post-body {
    font-size: 1rem;
    line-height: 1.6;
    margin-bottom: 15px;
    color: #e0e0e0;
}

.post-attached-image {
    max-width: 100%;
    border-radius: 8px;
    margin-bottom: 15px;
    border: 1px solid rgba(255, 255, 255, 0.1);
}

.post-footer-actions {
    display: flex;
    align-items: center;
    gap: 20px;
    border-top: 1px solid rgba(255, 255, 255, 0.05);
    padding-top: 12px;
}

.like-btn, .comment-btn {
    background: transparent;
    border: none;
    color: #a4b3b6;
    cursor: pointer;
    font-size: 0.95rem;
    display: flex;
    align-items: center;
    gap: 6px;
    transition: 0.3s;
}

.like-btn.active, .like-btn:hover {
    color: var(--danger-red);
}

.views-badge {
    margin-right: auto;
    font-size: 0.85rem;
    color: var(--neon-blue);
    display: flex;
    align-items: center;
    gap: 5px;
}

/* ============================================================
   # [👑 جديد] لوحة تحكم زوار الوطن العربي والأدمن
   # [EN] Admin Dashboard & Analytics Styling
   ============================================================ */
.admin-dashboard-section {
    background: #08090c;
    padding: 30px 5%;
    margin: 30px 5%;
    border-radius: 12px;
    border: 2px solid var(--text-gold);
}

.admin-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 20px;
    flex-wrap: wrap;
    gap: 10px;
}

.admin-badge {
    background: var(--text-gold);
    color: #0b0c10;
    padding: 6px 14px;
    border-radius: 15px;
    font-weight: bold;
    font-size: 0.85rem;
}

.analytics-cards .stat-card {
    background: var(--container-bg);
    padding: 20px;
    border-radius: 8px;
    text-align: center;
    border: 1px solid rgba(102, 252, 241, 0.2);
}

.stat-value {
    font-size: 1.6rem;
    font-weight: bold;
    color: var(--neon-blue);
    margin-top: 10px;
}

/* ============================================================
   # [📱 جديد] نافذة تسجيل الدخول برقم الهاتف (Modal Popup)
   # [EN] Modal Overlay & Form Dialogs
   ============================================================ */
.modal-overlay {
    position: fixed;
    top: 0; left: 0; width: 100%; height: 100%;
    background: rgba(0, 0, 0, 0.8);
    backdrop-filter: blur(5px);
    z-index: 9999;
    display: flex;
    justify-content: center;
    align-items: center;
    padding: 20px;
}

.modal-content {
    background: var(--container-bg);
    border: 2px solid var(--text-gold);
    border-radius: 12px;
    padding: 30px;
    width: 100%;
    max-width: 420px;
    position: relative;
    text-align: center;
}

.close-modal {
    position: absolute;
    top: 12px; left: 15px;
    font-size: 1.5rem;
    color: #a4b3b6;
    cursor: pointer;
    transition: 0.3s;
}

.close-modal:hover {
    color: var(--danger-red);
}

.modal-content h3 {
    margin-bottom: 10px;
    color: var(--text-gold);
}

.modal-content p {
    font-size: 0.9rem;
    color: #a4b3b6;
    margin-bottom: 20px;
}

.input-group {
    position: relative;
    margin-bottom: 20px;
}

.input-group i {
    position: absolute;
    right: 12px; top: 50%;
    transform: translateY(-50%);
    color: var(--text-gold);
}

.input-group input {
    width: 100%;
    padding: 12px 40px 12px 12px;
    background: #0b0c10;
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: 6px;
    color: #ffffff;
    font-family: var(--font-ar);
    font-size: 1rem;
    outline: none;
}

.input-group input:focus {
    border-color: var(--neon-blue);
}

.auth-submit-btn {
    width: 100%;
    background: linear-gradient(45deg, var(--teal-color), var(--neon-blue));
    color: #0b0c10;
    border: none;
    padding: 12px;
    font-size: 1rem;
    font-weight: bold;
    border-radius: 6px;
    cursor: pointer;
    transition: 0.3s;
}

.auth-submit-btn:hover {
    box-shadow: 0 0 15px rgba(102, 252, 241, 0.5);
}

/* ============================================================
   # [AR] أقسام المحتوى والميزات وشبكات العرض التوضيحية
   # [EN] Platform Features & Risk Disclaimers
   ============================================================ */
section, .fair-algorithm {
    padding: 40px 5%; margin: 20px 5%; border-radius: 10px;
    background-color: rgba(31, 40, 51, 0.5);
}

h2, h3 { color: var(--text-gold); }

.soon-lbl { font-weight: 700; font-size: 0.9rem; color: var(--text-gold); }

.grid-3 {
    display: grid; grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    gap: 20px; margin-top: 20px;
}

.grid-3 div, .gift-card {
    background-color: var(--container-bg); padding: 20px;
    border-radius: 8px; border-left: 4px solid var(--text-gold);
}

html[dir="ltr"] .grid-3 div, html[dir="ltr"] .gift-card { 
    border-left: none; 
    border-right: 4px solid var(--text-gold); 
}

/* ============================================================
   # [AR] تذييل الصفحة وروابط البورتفوليو وشبكات التواصل
   # [EN] Footer Navigation & Social Anchor Frameworks
   ============================================================ */
footer {
    background-color: #050608; padding: 40px 5% 20px 5%; text-align: center;
    border-top: 2px solid var(--container-bg);
}

.footer-symbols { 
    font-size: 1.2rem; 
    color: var(--text-gold); 
    letter-spacing: 5px; 
    margin-bottom: 20px; 
}

.footer-links { 
    display: flex; 
    flex-wrap: wrap; 
    justify-content: center; 
    gap: 15px; 
    margin-bottom: 25px; 
}

.footer-links a { 
    color: #a4b3b6; 
    text-decoration: none; 
    font-size: 0.9rem; 
    transition: 0.3s; 
    display: flex; 
    align-items: center; 
    gap: 5px; 
}

.footer-links a:hover { 
    color: var(--neon-blue); 
}

/* ============================================================
   # [AR] قواعد الشاشات سريعة الاستجابة (Responsive Media Queries)
   # [EN] Fluid Layout Media Queries for Tablets & Mobile Displays
   ============================================================ */
@media (max-width: 768px) {
    .hero-title { font-size: 1.8rem; }
    .hero-sub { font-size: 1.1rem; }
    .live-dashboard {
        grid-template-columns: 1fr;
    }
    .hero-buttons-container {
        flex-direction: column;
        align-items: center;
    }
    #hero-telegram-btn, #hero-channel-btn {
        width: 100%;
        max-width: 320px;
        text-align: center;
    }
    .forum-section {
        margin: 15px 2%;
        padding: 20px 3%;
    }
    .views-badge {
        margin-right: 0;
        margin-top: 5px;
    }
    .post-footer-actions {
        flex-wrap: wrap;
    }
}